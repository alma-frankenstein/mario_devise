<div>
<h2>Welcome to Mario's marvelous grocery store! Please see our list of products below. You can click a product to read its reviews</h2>
</div>

<div>
<h3>Most recently added products:</h3>
  <ul>
    <% @most_recent.each do |product| %>
      <li><%= product.name %></li>
    <% end %>
  </ul>
</div>

<div>
 <h3>Most reviewed product:</h3> 
 <p> <%= @most_reviewed %><p>   
</div>

 <h2>Products:</h2>

<% if @products.any? %>
  <ul>
    <% @products.each do |product| %>
      <li><%= link_to product.name, product_path(product) %> from <%= product.country_of_origin%>, $<%=product.cost%></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no products yet.</p>
<% end %>


<p><%= link_to "Add new product", new_product_path %></p>